[project]
name = "relaxweb"
version = "0.1.0"
description = "Self-hosted live room and game hall"
requires-python = ">=3.9"
dependencies = [
    "jsonschema>=4.21,<5",
    "psutil>=7.2",
    "websockets>=11",
]

[project.optional-dependencies]
assets = [
    "Pillow>=9",
]

[tool.uv]
package = false
