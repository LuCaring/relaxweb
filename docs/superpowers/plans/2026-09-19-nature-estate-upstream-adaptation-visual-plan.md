# 休闲庄园上游适配与美术升级实施计划

1. 将功能分支重放到最新 `origin/main`，保存安全备份分支。
2. 先更新/新增测试，覆盖 `game-config` 中的单人游戏元数据、房间游戏过滤和通用视图路由。
3. 迁移大厅、核心路由与入口导入，解决 `chat_server.py` 和页面资源版本冲突。
4. 下载并核验 Kenney Tiny Farm/Tiny Town CC0 包，挑选实际需要的地形、建筑、植物、角色和装饰 PNG。
5. 增加精简的素材清单与加载器；将地图绘制切换为素材优先、Canvas 回退。
6. 调整地图构图、碰撞框、地点标牌、作物阶段和四方向角色动画。
7. 精修弹层、HUD、钓鱼和矿场，使其颜色与新素材一致。
8. 运行庄园及上游全套相关测试，进行桌面/手机 HTML 实测。
9. 更新 PR 分支与说明，保留经济数值 `draft` 状态。
