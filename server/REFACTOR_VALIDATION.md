# 服务端拆分本地验证（2026-09-22）

## 对照范围

以本轮开始时已完成前两步拆分的工作区为基线，先复制隔离快照并逐文件运行 Python
测试，再在完成剩余拆分后重复运行。所有数据库使用临时路径；协议使用本地监听，
浏览器测试使用本地静态服务。未连接生产服务，未提交或部署。

本轮将 `chat_server.py` 从 1601 行收拢至 17 行；65 种消息路由保持完整。
对迁移的 61 个函数做 AST 对照，除依赖引用改绑、方法的 `self` 和多行文本缩进外一致。
额外变化是应用关闭时明确取消并等待后台任务、关闭房间计时器；新增测试覆盖该顺序。
账号、结算等模块通过注入依赖隔离状态，遵循 `codebase-design` 的模块设计原则。

## 验证结果

- Python：逐文件执行全部 36 个 `tests/test_*.py`；最终一轮 29 个通过，7 个存在基线失败。
- 新增 `test_server_app.py` 的 10 项通过：实例/数据库隔离、65 种路由、连接锁、发送异常、
  在线人数、多连接同步、资料/转账/注销、断线恢复、任务清理、监听退出顺序和真实协议。
- `test_server_modules.py` 7 项通过，包括真实启动入口的托管退款、竞猜恢复和自动封盘。
- 德州统计 63 项、德州奖励 14 项、奖励 18 项、管理员 10 项、资产排行 5 项通过。
  结算失败回滚、重试幂等、退出退款和数据库快照测试均保留原断言。
- 庄园基础/拜访、竞猜、奖励、观战、麻将/掼蛋离桌协议通过。
- UNO 协议测试改为自建临时库和随机端口后通过，不再依赖预先启动的 8765 服务。
- 浏览器/JS：资产排行、段位排行榜、排行榜状态、每日奖励、德州奖励、竞猜 UI 共 6 个脚本通过；
  另 2 个脚本失败已在旧快照复现（见下）。状态测试需要 `node --experimental-vm-modules`。
- `compileall`、生产代码及新增测试的 `pyflakes`、`git diff --check` 通过。
- `live-test/reset.sh` 通过 shell 语法检查；仅在临时库执行其种子 Python 片段，成功生成账号。
  未执行该脚本的杀进程、删库或启动服务步骤。

## 保留的基线问题

本次是结构重构，未修改以下测试断言或业务规则来掩盖既有失败。

| 测试 | 基线与重构后共同现象 |
| --- | --- |
| `test_estate_activities.py`、`test_estate_activities_protocol.py` | 挖矿结束预期耐久 19，实际 14 |
| `test_estate_character_assets.py` | 皮肤集合已新增 malphite、ryu、nailong，测试仍使用旧集合 |
| `test_estate_skin_purchase.py`、`test_estate_skin_purchase_protocol.py`、`test_estate_skins.py` | 旧皮肤价格/集合/余额断言与当前目录不符 |
| `test_ratings.py` | 同一手离桌后重入测试期待 bob 重新入座，但当前协议不允许进行中入座 |
| `test_ratings.cjs` | `.rating-card` 匹配两个节点，触发 Playwright 严格模式错误 |
| `test_table_ui.cjs` | 320px 麻将牌架/牌河/玩家标签不重叠断言失败 |

另有两个随机相关的既有不稳定测试：`test_mahjong.py` 的“不足起和分不能胡”，以及
`test_table_regressions.py` 的抢杠基础番数断言。失败都在旧快照出现过，最终一轮二者通过；
没有通过调整游戏逻辑或放宽断言处理它们。

结论：本轮测试与基线对照未发现新增回归，但全仓测试并非全绿；以上既有问题需另行处理。

## 工作区提示

`live-test` 是独立 Git 子模块；本轮对其中 `reset.sh` 的修改也尚未提交。
后续提交时需要同时处理该子模块改动，不能只提交主仓库。
